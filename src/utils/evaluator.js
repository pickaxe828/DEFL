export class DEFLevaluator {
    constructor(region, sex, sex_ori, pref_age_range, age) {
        this.region = region
        this.sex = sex
        this.sex_ori = sex_ori
        this.pref_age_range = pref_age_range
        this.age = age
    }

    async evaluate() {
        
    }
}